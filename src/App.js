import React from 'react';
import { Header } from './Header'
import { Timeline } from './Timeline'

export default () => (
  <div>
    <header>
      <Header />
    </header>
    <main>
      <Timeline />
    </main>
  </div>
)
